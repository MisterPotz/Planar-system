package planar_structure.mechanism.process.argument

case class AnalysisArgs(z : List[Int], x : List[Double],
                        betas: List[Double], modules: List[Double],
                        alpha : List[Double],
                        //ca : List[Double], ha : List[Double]
                        satellites : Int)
